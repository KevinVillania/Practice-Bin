#include "Car.h"
#include <iostream>
#include <string>
using namespace std;


inputFunc(string model, string brand, int topSpeed, int carPrice){

        model = model;
        brand = brand;
        topSpeed = topSpeed;
        carPrice = carPrice;
}
